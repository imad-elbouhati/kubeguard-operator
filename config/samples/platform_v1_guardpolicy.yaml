apiVersion: platform.demo/v1
kind: GuardPolicy
metadata:
  labels:
    app.kubernetes.io/name: kubeguard-operator
    app.kubernetes.io/managed-by: kustomize
  name: guardpolicy-sample
spec:
  # TODO(user): Add fields here
